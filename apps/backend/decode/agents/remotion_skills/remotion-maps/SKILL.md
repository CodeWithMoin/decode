---
name: remotion-maps
description: Build geographic explanations with deterministic SVG map motion.
---

Use a simplified inline SVG map or route when geography carries the idea. Animate route reveal,
camera emphasis, markers, and labels from frames. Preserve geographic relationships and avoid
decorative map detail. Do not fetch tiles, use remote map SDKs, or depend on runtime network access.
